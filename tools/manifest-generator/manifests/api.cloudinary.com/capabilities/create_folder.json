{
  "name": "create_folder",
  "description": "Create a new folder in your Cloudinary media library to organize assets hierarchically. Nested folders are supported by using forward slashes in the path.",
  "endpoint": "/v1_1/{cloud_name}/folders/{folder_path}",
  "method": "POST",
  "parameters": [
    { "name": "folder_path", "type": "string", "description": "The folder path to create (URL path parameter). Use forward slashes for nesting.", "required": true, "example": "products/2024/summer" }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://api.cloudinary.com/v1_1/my_cloud/folders/products/2024/summer",
    "headers": {
      "Authorization": "Basic {base64(api_key:api_secret)}"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "success": true,
      "path": "products/2024/summer",
      "name": "summer"
    }
  }
}
