{
  "name": "redirect_urls_get",
  "resource_group": "redirect_urls",
  "description": "Retrieve a single redirect URL by its ID.",
  "endpoint": "/v1/redirect_urls/{redirect_url_id}",
  "method": "GET",
  "parameters": [
    {
      "name": "redirect_url_id",
      "type": "string",
      "required": true,
      "description": "Redirect URL ID.",
      "in": "path"
    }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://api.clerk.com/v1/redirect_urls/ru_abc",
    "headers": {
      "Authorization": "Bearer sk_live_xxxxx"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "id": "ru_abc",
      "url": "https://app.example.com/callback"
    }
  }
}