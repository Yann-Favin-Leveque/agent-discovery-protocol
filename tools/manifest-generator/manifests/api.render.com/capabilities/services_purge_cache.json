{
  "name": "services_purge_cache",
  "description": "Purge the build cache for a service to force a clean rebuild on the next deploy.",
  "resource_group": "services",
  "endpoint": "/v1/services/{serviceId}/cache/purge",
  "method": "POST",
  "parameters": [
    {
      "name": "serviceId",
      "type": "string",
      "required": true,
      "description": "The service ID to purge cache for.",
      "in": "path"
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://api.render.com/v1/services/srv-a1b2c3d4e5/cache/purge"
  },
  "response_example": null
}
