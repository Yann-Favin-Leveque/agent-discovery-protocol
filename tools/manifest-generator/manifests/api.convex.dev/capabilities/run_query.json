{
  "name": "run_query",
  "description": "Execute a read-only query function against your Convex backend. Queries are deterministic functions that read from the database and return results.",
  "endpoint": "/api/query",
  "method": "POST",
  "parameters": [
    { "name": "path", "type": "string", "description": "The path to the query function (e.g., 'messages:list').", "required": true, "example": "messages:list" },
    { "name": "args", "type": "object", "description": "Arguments to pass to the query function.", "required": false, "example": { "limit": 10 } }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://your-deployment.convex.cloud/api/query",
    "headers": { "Authorization": "Convex {deploy_key}", "Content-Type": "application/json" },
    "body": { "path": "messages:list", "args": { "limit": 10 } }
  },
  "response_example": {
    "status": 200,
    "body": { "status": "success", "value": [{ "_id": "msg_123", "text": "Hello world", "author": "user_456" }] }
  }
}
