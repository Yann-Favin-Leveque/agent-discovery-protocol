{
  "name": "training_cancel",
  "description": "Cancel a running training job.",
  "resource_group": "trainings",
  "endpoint": "/trainings/{training_id}/cancel",
  "method": "POST",
  "parameters": [{"name": "training_id", "type": "string", "required": true, "description": "The training ID to cancel."}],
  "request_example": {"method": "POST", "url": "https://api.replicate.com/v1/trainings/train123/cancel", "headers": {"Authorization": "Bearer r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"}},
  "response_example": {"status": 200, "body": {"id": "train123", "status": "canceled"}}
}
