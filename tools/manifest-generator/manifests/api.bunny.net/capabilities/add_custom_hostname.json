{
  "name": "add_custom_hostname",
  "description": "Add a custom hostname (domain) to a pull zone and configure SSL. Bunny automatically provisions and renews free SSL certificates.",
  "endpoint": "/pullzone/{id}/addHostname",
  "method": "POST",
  "parameters": [
    {
      "name": "id",
      "type": "integer",
      "description": "The ID of the pull zone to add the hostname to.",
      "required": true,
      "example": 123456
    },
    {
      "name": "Hostname",
      "type": "string",
      "description": "The custom hostname to add. Must have a CNAME DNS record pointing to the pull zone hostname.",
      "required": true,
      "example": "cdn.example.com"
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://api.bunny.net/pullzone/123456/addHostname",
    "headers": {
      "AccessKey": "{api_key}",
      "Content-Type": "application/json"
    },
    "body": {
      "Hostname": "cdn.example.com"
    }
  },
  "response_example": {
    "status": 204,
    "body": {}
  }
}
