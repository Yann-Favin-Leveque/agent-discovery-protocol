{
  "name": "workflow_states_list",
  "description": "List workflow states for a team.",
  "resource_group": "workflow_states",
  "endpoint": "/graphql",
  "method": "POST",
  "parameters": [
    {
      "name": "filter",
      "type": "object",
      "required": false,
      "location": "body",
      "description": "Filter by team"
    }
  ],
  "auth_scopes": [
    "read",
    "write",
    "issues:create",
    "issues:write"
  ],
  "request_example": {
    "query": "{ workflowStates(filter: { team: { key: { eq: \"ENG\" } } }) { nodes { id name color type position } } }"
  }
}