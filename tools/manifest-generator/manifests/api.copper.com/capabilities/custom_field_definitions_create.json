{
  "name": "custom_field_definitions_create",
  "description": "Create a new custom field definition.",
  "resource_group": "custom_fields",
  "endpoint": "/custom_field_definitions",
  "method": "POST",
  "parameters": [
    {"name": "name", "type": "string", "required": true, "description": "Name of the custom field"},
    {"name": "data_type", "type": "string", "required": true, "description": "Data type (String, Text, Dropdown, Date, etc.)"},
    {"name": "available_on", "type": "array", "required": false, "description": "Entity types the field is available on"}
  ],
  "request_example": {"name": "Industry", "data_type": "Dropdown", "available_on": ["lead", "person"]},
  "response_example": {"id": 1, "name": "Industry", "data_type": "Dropdown"},
  "auth_scopes": []
}
