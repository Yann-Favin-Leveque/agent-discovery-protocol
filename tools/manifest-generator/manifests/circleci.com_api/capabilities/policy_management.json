{
  "name": "policy_management",
  "description": "Manage policy management — makes a decision, creates policy bundle for the context, retrieves the owner's decision audit logs. Supports standard CRUD operations through the REST API.",
  "endpoint": "/owner/{ownerID}/context/{context}/decision",
  "method": "POST",
  "parameters": [
    {
      "name": "input",
      "type": "string",
      "description": "The input field.",
      "required": true,
      "example": "example_value"
    },
    {
      "name": "metadata",
      "type": "object",
      "description": "The metadata field.",
      "required": false,
      "example": {}
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://circleci.com/api/v2/owner/{ownerID}/context/{context}/decision",
    "headers": {
      "Content-Type": "application/json",
      "Circle-Token": "{api_key}"
    },
    "body": {
      "input": "example_value"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {
        "input": "example_value"
      }
    }
  }
}
