{
  "name": "sessions_generate_context",
  "description": "Generate an AI-powered context analysis for a session, summarizing user behavior and key interactions.",
  "endpoint": "/v2/sessions/{session_id}/context",
  "method": "POST",
  "resource_group": "sessions",
  "parameters": [
    {
      "name": "session_id",
      "type": "string",
      "description": "The session ID to generate context for.",
      "required": true,
      "example": "sess_abc123",
      "in": "path"
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://api.fullstory.com/v2/sessions/sess_abc123/context",
    "headers": {
      "Authorization": "Basic {api_key}",
      "Content-Type": "application/json"
    },
    "body": {}
  },
  "response_example": {
    "status": 200,
    "body": {
      "context": "User visited the pricing page, compared plans, experienced a rage click on the checkout button, and eventually abandoned the session without purchasing."
    }
  },
  "auth_scopes": [
    "server"
  ],
  "rate_limits": {
    "requests_per_minute": 60
  }
}
