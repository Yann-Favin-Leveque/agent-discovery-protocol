{
  "name": "cards_labels_list",
  "description": "List labels on a card.",
  "resource_group": "cards",
  "endpoint": "/cards/{id}/labels",
  "method": "GET",
  "parameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "The ID of the card"
    }
  ],
  "auth_scopes": [
    "read",
    "write"
  ]
}