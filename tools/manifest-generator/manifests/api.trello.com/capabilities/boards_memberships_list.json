{
  "name": "boards_memberships_list",
  "description": "Get board memberships.",
  "resource_group": "boards",
  "endpoint": "/boards/{id}/memberships",
  "method": "GET",
  "parameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "The ID of the board"
    }
  ],
  "auth_scopes": [
    "read",
    "write"
  ]
}