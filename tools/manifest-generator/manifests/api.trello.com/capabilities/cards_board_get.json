{
  "name": "cards_board_get",
  "description": "Get the board a card is on.",
  "resource_group": "cards",
  "endpoint": "/cards/{id}/board",
  "method": "GET",
  "parameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "The ID of the card"
    }
  ],
  "auth_scopes": [
    "read",
    "write"
  ]
}