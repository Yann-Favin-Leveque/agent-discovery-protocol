{
  "name": "lists_get",
  "description": "Get a list by ID.",
  "resource_group": "lists",
  "endpoint": "/lists/{id}",
  "method": "GET",
  "parameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "The ID of the list"
    }
  ],
  "auth_scopes": [
    "read",
    "write"
  ]
}