{
  "name": "batch",
  "description": "Make multiple GET requests in a single call.",
  "resource_group": "batch",
  "endpoint": "/batch",
  "method": "GET",
  "parameters": [
    {
      "name": "urls",
      "type": "string",
      "required": true,
      "location": "query",
      "description": "Comma-separated list of API paths to call"
    }
  ],
  "auth_scopes": [
    "read",
    "write"
  ]
}