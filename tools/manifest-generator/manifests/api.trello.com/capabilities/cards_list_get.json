{
  "name": "cards_list_get",
  "description": "Get the list a card is in.",
  "resource_group": "cards",
  "endpoint": "/cards/{id}/list",
  "method": "GET",
  "parameters": [
    {
      "name": "id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "The ID of the card"
    }
  ],
  "auth_scopes": [
    "read",
    "write"
  ]
}