{
  "name": "embedded_sending_link_get",
  "description": "Generate an embedded sending link to embed the document preparation and sending UI directly in your application.",
  "endpoint": "/v1/document/getEmbeddedSendLink",
  "method": "GET",
  "resource_group": "embedded",
  "parameters": [
    {
      "name": "documentId",
      "type": "string",
      "required": true,
      "description": "The draft document ID to generate a sending link for (query parameter).",
      "in": "query"
    },
    {
      "name": "redirectUrl",
      "type": "string",
      "required": false,
      "description": "URL to redirect to after sending is complete.",
      "in": "query"
    }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://api.boldsign.com/v1/document/getEmbeddedSendLink?documentId=doc_draft_123",
    "headers": {
      "X-API-KEY": "{api_key}"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "sendLink": "https://app.boldsign.com/document/prepare/draft_123?token=abc456"
    }
  },
  "auth_scopes": [],
  "rate_limits": {
    "requests_per_minute": 100
  }
}
