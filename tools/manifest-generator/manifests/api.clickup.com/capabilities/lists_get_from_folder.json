{
  "name": "lists_get_from_folder",
  "description": "Get lists in a folder.",
  "resource_group": "lists",
  "endpoint": "/api/v2/folder/{folder_id}/list",
  "method": "GET",
  "parameters": [
    {
      "name": "folder_id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Folder ID"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}