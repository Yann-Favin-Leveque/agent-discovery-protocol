{
  "spec_version": "1.0",
  "name": "Neon API",
  "description": "The Neon API allows you to access and manage Neon programmatically. You can use the Neon API to manage API keys, projects, branches, compute endpoints, databases, roles, and operations. For information about these features, refer to the [Neon documentation](https://neon.tech/docs/manage/overview/).\n\nYou can run Neon API requests from this API reference using the **Try It** feature. Enter your API key as a **Bearer** token in the **Authorization** section of the page.",
  "base_url": "https://console.neon.tech/api/v2",
  "auth": {
    "type": "api_key",
    "header": "Authorization",
    "prefix": "Bearer"
  },
  "capabilities": [
    {
      "name": "project",
      "description": "Manage project — create project, recover a deleted project, grant project access. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/project"
    },
    {
      "name": "api_key",
      "description": "Manage api key — create api key, list api keys, revoke api key. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/api_key"
    },
    {
      "name": "operation",
      "description": "Manage operation — list operations, retrieve operation details. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/operation"
    },
    {
      "name": "data_api",
      "description": "Manage data api — create neon data api, get neon data api, update neon data api. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/data_api"
    },
    {
      "name": "auth",
      "description": "Manage auth — enable neon auth for the branch, add domain to redirect_uri whitelist, create new auth user. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/auth"
    },
    {
      "name": "auth_legacy",
      "description": "Manage auth legacy — create auth provider sdk keys, transfer neon-managed auth project to your own account. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/auth_legacy"
    },
    {
      "name": "branch",
      "description": "Manage branch — create branch, create anonymized branch, restore branch. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/branch"
    },
    {
      "name": "endpoint",
      "description": "Manage endpoint — create compute endpoint, start compute endpoint, suspend compute endpoint. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/endpoint"
    },
    {
      "name": "consumption",
      "description": "Manage consumption — retrieve account consumption metrics (legacy plans), retrieve project consumption metrics (legacy plans), retrieve project consumption metrics. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/consumption"
    },
    {
      "name": "organizations",
      "description": "Manage organizations — create organization api key, create organization invitations, transfer projects between organizations. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/organizations"
    },
    {
      "name": "region",
      "description": "Manage region — list supported regions. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/region"
    },
    {
      "name": "users",
      "description": "Manage users — transfer projects from personal account to organization, retrieve current user details, retrieve current user organizations list. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/users"
    }
  ]
}
