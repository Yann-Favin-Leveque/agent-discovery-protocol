{
  "name": "envelope_publish",
  "description": "Manage envelope publish — submits a batch of historical envelopes for republish to a webhook. Supports standard CRUD operations through the REST API.",
  "endpoint": "/v2.1/accounts/{accountId}/connect/envelopes/publish/historical",
  "method": "POST",
  "parameters": [
    {
      "name": "accountId",
      "type": "string",
      "description": "The external account number (int) or account ID GUID.",
      "required": true,
      "example": "abc_123"
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://www.docusign.net/restapi/v2.1/accounts/{accountId}/connect/envelopes/publish/historical",
    "headers": {
      "Content-Type": "application/json"
    },
    "body": {
      "accountId": "abc_123"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {
        "accountId": "abc_123"
      }
    }
  }
}
