{
  "name": "chunked_uploads",
  "description": "Manage chunked uploads — initiate a new chunked upload, commit a chunked upload, add a chunk to an existing chunked upload. Supports standard CRUD operations through the REST API.",
  "endpoint": "/v2.1/accounts/{accountId}/chunked_uploads",
  "method": "POST",
  "parameters": [
    {
      "name": "accountId",
      "type": "string",
      "description": "The external account number (int) or account ID GUID.",
      "required": true,
      "example": "abc_123"
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://www.docusign.net/restapi/v2.1/accounts/{accountId}/chunked_uploads",
    "headers": {
      "Content-Type": "application/json"
    },
    "body": {
      "accountId": "abc_123"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {
        "accountId": "abc_123"
      }
    }
  }
}
