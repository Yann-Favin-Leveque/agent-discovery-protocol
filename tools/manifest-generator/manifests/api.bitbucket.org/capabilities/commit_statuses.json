{
  "name": "commit_statuses",
  "description": "Manage commit statuses — create a build status for a commit, update a build status for a commit, list commit statuses for a commit. Supports standard CRUD operations through the REST API.",
  "endpoint": "/repositories/{workspace}/{repo_slug}/commit/{commit}/statuses/build",
  "method": "POST",
  "parameters": [],
  "request_example": {
    "method": "POST",
    "url": "https://api.bitbucket.org/2.0/repositories/{workspace}/{repo_slug}/commit/{commit}/statuses/build",
    "headers": {
      "Content-Type": "application/json",
      "Authorization": "Bearer {access_token}"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {}
    }
  },
  "auth_scopes": [
    "repository",
    "repository:write",
    "repository:admin",
    "repository:delete",
    "project"
  ]
}
