{
  "name": "pipelines",
  "description": "Manage pipelines — create workspace runner, create a variable for a workspace, run a pipeline. Supports standard CRUD operations through the REST API.",
  "endpoint": "/workspaces/{workspace}/pipelines-config/runners",
  "method": "POST",
  "parameters": [
    {
      "name": "workspace",
      "type": "string",
      "description": "This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example `{workspace UUID}`.",
      "required": true,
      "example": "example_value"
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://api.bitbucket.org/2.0/workspaces/{workspace}/pipelines-config/runners",
    "headers": {
      "Content-Type": "application/json",
      "Authorization": "Bearer {access_token}"
    },
    "body": {
      "workspace": "example_value"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {
        "workspace": "example_value"
      }
    }
  },
  "auth_scopes": [
    "repository",
    "repository:write",
    "repository:admin",
    "repository:delete",
    "project"
  ]
}
