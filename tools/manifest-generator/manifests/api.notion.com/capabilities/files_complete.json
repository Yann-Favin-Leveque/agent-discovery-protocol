{
  "name": "files_complete",
  "description": "Finalize a file upload.",
  "resource_group": "files",
  "endpoint": "/v1/files/{file_id}/complete",
  "method": "POST",
  "parameters": [
    {
      "name": "file_id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "The ID of the file upload",
      "in": "path"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}
