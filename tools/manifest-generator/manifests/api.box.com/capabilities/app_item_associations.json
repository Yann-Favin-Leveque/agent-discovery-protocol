{
  "name": "app_item_associations",
  "description": "Manage app item associations — list file app item associations, list folder app item associations. Supports standard CRUD operations through the REST API.",
  "endpoint": "/files/{file_id}/app_item_associations",
  "method": "GET",
  "parameters": [
    {
      "name": "file_id",
      "type": "string",
      "description": "The unique identifier that represents a file.\n\nThe ID for any file can be determined\nby visiting a file in the web application\nand copying the ID from the URL. For example,\nfor the URL `https://*.app.box.com/files/123`\nthe `file_id` is `123`.",
      "required": true,
      "example": "12345"
    },
    {
      "name": "limit",
      "type": "number",
      "description": "The maximum number of items to return per page.",
      "required": false,
      "example": 1000
    },
    {
      "name": "marker",
      "type": "string",
      "description": "Defines the position marker at which to begin returning results. This is\nused when paginating using marker-based pagination.\n\nThis requires `usemarker` to be set to `true`.",
      "required": false,
      "example": "JV9IRGZmieiBasejOG9yDCRNgd2ymoZIbjsxbJMjIs3kioVii"
    },
    {
      "name": "application_type",
      "type": "string",
      "description": "If given, only return app items for this application type.",
      "required": false,
      "example": "hubs"
    }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://api.box.com/2.0/files/{file_id}/app_item_associations",
    "headers": {
      "Content-Type": "application/json",
      "Authorization": "Bearer {access_token}"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "success": true,
      "data": {
        "file_id": "12345"
      }
    }
  },
  "auth_scopes": [
    "root_readonly",
    "root_readwrite",
    "manage_app_users",
    "manage_managed_users",
    "manage_groups"
  ]
}
