{
  "spec_version": "1.0",
  "name": "Stripe API",
  "description": "The Stripe REST API. Please see https://stripe.com/docs/api for more details.",
  "base_url": "https://api.stripe.com",
  "auth": {
    "type": "api_key",
    "header": "Authorization",
    "prefix": "Bearer"
  },
  "capabilities": [
    {
      "name": "account",
      "description": "Manage account — create an account link, create an account session, retrieve account. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/account"
    },
    {
      "name": "accounts",
      "description": "Manage accounts — <p>with <a href=\"/docs/connect\">connect</a>, you can create stripe accounts for your users.\nto do th, update an account, create an external account. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/accounts"
    },
    {
      "name": "apple_pay",
      "description": "Manage apple pay — <p>create an apple pay domain.</p>, <p>list apple pay domains.</p>, <p>retrieve an apple pay domain.</p>. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/apple_pay"
    },
    {
      "name": "application_fees",
      "description": "Manage application fees — create an application fee refund, update an application fee refund, list all application fees. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/application_fees"
    },
    {
      "name": "apps",
      "description": "Manage apps — set a secret, delete a secret, list secrets. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/apps"
    },
    {
      "name": "balance",
      "description": "Manage balance — retrieve balance, list all balance transactions, retrieve a balance transaction. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/balance"
    },
    {
      "name": "balance_settings",
      "description": "Manage balance settings — update balance settings, retrieve balance settings. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/balance_settings"
    },
    {
      "name": "balance_transactions",
      "description": "Manage balance transactions — list all balance transactions, retrieve a balance transaction. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/balance_transactions"
    },
    {
      "name": "billing",
      "description": "Manage billing — create a billing alert, create a credit grant, create a billing meter event adjustment. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/billing"
    },
    {
      "name": "billing_portal",
      "description": "Manage billing portal — create a portal configuration, create a portal session, update a portal configuration. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/billing_portal"
    },
    {
      "name": "charges",
      "description": "Manage charges — <p>this method is no longer recommended—use the <a href=\"/docs/api/payment_intents\">payment intents , update a charge, capture a payment. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/charges"
    },
    {
      "name": "checkout",
      "description": "Manage checkout — create a checkout session, update a checkout session, expire a checkout session. Supports standard CRUD operations through the REST API.",
      "detail_url": "/capabilities/checkout"
    }
  ]
}
