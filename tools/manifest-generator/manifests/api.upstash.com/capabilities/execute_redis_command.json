{
  "name": "execute_redis_command",
  "description": "Execute any Redis command over HTTP against your Upstash Redis database. Supports all standard Redis commands like GET, SET, HSET, LPUSH, etc. Each database has a unique REST URL.",
  "endpoint": "/{command}/{args}",
  "method": "GET",
  "parameters": [
    { "name": "command", "type": "string", "description": "The Redis command to execute (e.g., GET, SET, HSET).", "required": true, "example": "SET" },
    { "name": "args", "type": "string", "description": "Command arguments separated by /.", "required": true, "example": "mykey/myvalue" }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://{your-endpoint}.upstash.io/SET/mykey/myvalue",
    "headers": { "Authorization": "Bearer {redis_token}" }
  },
  "response_example": {
    "status": 200,
    "body": { "result": "OK" }
  }
}
