{
  "name": "real_time_scheduling_disable",
  "description": "Disable an active real-time scheduling link so it can no longer be used for booking.",
  "endpoint": "/v1/real_time_scheduling/{real_time_scheduling_id}/disable",
  "method": "POST",
  "resource_group": "scheduling",
  "parameters": [
    {
      "name": "real_time_scheduling_id",
      "type": "string",
      "required": true,
      "description": "The real-time scheduling link ID to disable (path parameter)."
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://api.cronofy.com/v1/real_time_scheduling/rts_abc123/disable",
    "headers": {
      "Authorization": "Bearer {api_key}"
    }
  },
  "response_example": {
    "status": 202,
    "body": {}
  },
  "auth_scopes": ["create_event"],
  "rate_limits": {
    "requests_per_minute": 60
  }
}
