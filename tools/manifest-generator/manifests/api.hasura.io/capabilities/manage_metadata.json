{
  "name": "manage_metadata",
  "description": "Track tables, set up relationships, and configure permissions via the Hasura metadata API. This is how you tell Hasura which database objects to expose through GraphQL.",
  "endpoint": "/v1/metadata",
  "method": "POST",
  "parameters": [
    { "name": "type", "type": "string", "description": "The metadata operation type (e.g., pg_track_table, pg_create_object_relationship).", "required": true, "example": "pg_track_table" },
    { "name": "args", "type": "object", "description": "Arguments specific to the operation type.", "required": true, "example": { "source": "default", "table": { "schema": "public", "name": "users" } } }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://your-project.hasura.app/v1/metadata",
    "headers": { "x-hasura-admin-secret": "{admin_secret}", "Content-Type": "application/json" },
    "body": { "type": "pg_track_table", "args": { "source": "default", "table": { "schema": "public", "name": "users" } } }
  },
  "response_example": {
    "status": 200,
    "body": { "message": "success" }
  }
}
