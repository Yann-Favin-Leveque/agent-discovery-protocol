{
  "name": "run_graphql_query",
  "description": "Execute GraphQL queries and mutations against your Hasura endpoint. Hasura auto-generates a full GraphQL schema from your database tables.",
  "endpoint": "/v1/graphql",
  "method": "POST",
  "parameters": [
    { "name": "query", "type": "string", "description": "The GraphQL query or mutation string.", "required": true, "example": "query { users { id name email } }" },
    { "name": "variables", "type": "object", "description": "Variables for the GraphQL query.", "required": false, "example": {} },
    { "name": "operationName", "type": "string", "description": "Name of the operation to execute.", "required": false, "example": "GetUsers" }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://your-project.hasura.app/v1/graphql",
    "headers": { "x-hasura-admin-secret": "{admin_secret}", "Content-Type": "application/json" },
    "body": { "query": "query { users { id name email } }" }
  },
  "response_example": {
    "status": 200,
    "body": { "data": { "users": [{ "id": 1, "name": "John", "email": "john@example.com" }] } }
  }
}
