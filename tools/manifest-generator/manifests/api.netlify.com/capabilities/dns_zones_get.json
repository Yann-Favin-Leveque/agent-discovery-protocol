{
  "name": "dns_zones_get",
  "description": "Get details of a specific DNS zone.",
  "endpoint": "/api/v1/dns_zones/{zone_id}",
  "method": "GET",
  "resource_group": "dns",
  "parameters": [
    {
      "name": "zone_id",
      "type": "string",
      "description": "DNS zone ID",
      "required": true,
      "example": "zone_123",
      "in": "path"
    }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://api.netlify.com/api/v1/dns_zones/{zone_id}",
    "headers": {
      "Authorization": "Bearer {token}"
    },
    "body": {}
  },
  "response_example": {
    "status": 200,
    "body": {}
  },
  "auth_scopes": [],
  "rate_limits": {
    "requests_per_minute": 200
  }
}
