{
  "name": "tasks_projects_list",
  "description": "Get projects a task is in.",
  "resource_group": "tasks",
  "endpoint": "/1.0/tasks/{task_gid}/projects",
  "method": "GET",
  "parameters": [
    {
      "name": "task_gid",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Task GID",
      "in": "path"
    },
    {
      "name": "opt_fields",
      "type": "string",
      "required": false,
      "location": "query",
      "description": "Comma-separated list of fields to include",
      "in": "query"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}
