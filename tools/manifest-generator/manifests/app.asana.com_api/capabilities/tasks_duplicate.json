{
  "name": "tasks_duplicate",
  "description": "Duplicate a task.",
  "resource_group": "tasks",
  "endpoint": "/1.0/tasks/{task_gid}/duplicate",
  "method": "POST",
  "parameters": [
    {
      "name": "task_gid",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Task GID"
    },
    {
      "name": "data",
      "type": "object",
      "required": true,
      "location": "body",
      "description": "Duplication options (name, include fields)"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}