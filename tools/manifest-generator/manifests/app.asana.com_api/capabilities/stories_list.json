{
  "name": "stories_list",
  "description": "Get stories (comments) on a task.",
  "resource_group": "stories",
  "endpoint": "/1.0/tasks/{task_gid}/stories",
  "method": "GET",
  "parameters": [
    {
      "name": "task_gid",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Task GID"
    },
    {
      "name": "opt_fields",
      "type": "string",
      "required": false,
      "location": "query",
      "description": "Comma-separated list of fields to include"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}