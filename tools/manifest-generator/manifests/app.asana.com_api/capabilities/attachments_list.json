{
  "name": "attachments_list",
  "description": "Get attachments on an object.",
  "resource_group": "attachments",
  "endpoint": "/1.0/tasks/{task_gid}/attachments",
  "method": "GET",
  "parameters": [
    {
      "name": "task_gid",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Task GID"
    },
    {
      "name": "opt_fields",
      "type": "string",
      "required": false,
      "location": "query",
      "description": "Comma-separated list of fields to include"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}