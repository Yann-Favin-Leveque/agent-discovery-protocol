{
  "name": "batch",
  "description": "Submit parallel requests.",
  "resource_group": "batch",
  "endpoint": "/1.0/batch",
  "method": "POST",
  "parameters": [
    {
      "name": "data",
      "type": "object",
      "required": true,
      "location": "body",
      "description": "Batch request data with actions array"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}