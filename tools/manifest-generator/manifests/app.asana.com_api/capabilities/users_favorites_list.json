{
  "name": "users_favorites_list",
  "description": "Get a user's favorites.",
  "resource_group": "users",
  "endpoint": "/1.0/users/{user_gid}/favorites",
  "method": "GET",
  "parameters": [
    {
      "name": "user_gid",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "User GID"
    },
    {
      "name": "workspace",
      "type": "string",
      "required": true,
      "location": "query",
      "description": "Workspace GID"
    },
    {
      "name": "resource_type",
      "type": "string",
      "required": true,
      "location": "query",
      "description": "Filter by type"
    },
    {
      "name": "opt_fields",
      "type": "string",
      "required": false,
      "location": "query",
      "description": "Comma-separated list of fields to include"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}