{
  "name": "teams_list",
  "description": "List teams in a workspace.",
  "resource_group": "teams",
  "endpoint": "/1.0/workspaces/{workspace_gid}/teams",
  "method": "GET",
  "parameters": [
    {
      "name": "workspace_gid",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Workspace GID"
    },
    {
      "name": "opt_fields",
      "type": "string",
      "required": false,
      "location": "query",
      "description": "Comma-separated list of fields to include"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}