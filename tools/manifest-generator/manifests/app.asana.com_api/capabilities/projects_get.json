{
  "name": "projects_get",
  "description": "Get a project by GID.",
  "resource_group": "projects",
  "endpoint": "/1.0/projects/{project_gid}",
  "method": "GET",
  "parameters": [
    {
      "name": "project_gid",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Project GID"
    },
    {
      "name": "opt_fields",
      "type": "string",
      "required": false,
      "location": "query",
      "description": "Comma-separated list of fields to include"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}