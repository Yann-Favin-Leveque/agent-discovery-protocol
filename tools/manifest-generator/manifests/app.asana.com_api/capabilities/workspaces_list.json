{
  "name": "workspaces_list",
  "description": "Get all workspaces.",
  "resource_group": "workspaces",
  "endpoint": "/1.0/workspaces",
  "method": "GET",
  "parameters": [
    {
      "name": "opt_fields",
      "type": "string",
      "required": false,
      "location": "query",
      "description": "Comma-separated list of fields to include"
    }
  ],
  "auth_scopes": [
    "default"
  ]
}