{
  "name": "chat_completion_create",
  "description": "Generate a model response for a conversation. Provide a list of messages with roles (system, user, assistant) and the model will generate the next assistant message. Supports tool/function calling, JSON mode, vision (image inputs), streaming, and configurable sampling parameters.",
  "resource_group": "chat",
  "endpoint": "/v1/chat/completions",
  "method": "POST",
  "parameters": [
    {
      "name": "model",
      "type": "string",
      "required": true,
      "description": "Model ID to use (e.g. 'gpt-4o', 'gpt-4o-mini', 'gpt-4-turbo', 'gpt-3.5-turbo')."
    },
    {
      "name": "messages",
      "type": "array",
      "required": true,
      "description": "Array of message objects with 'role' (system/user/assistant/tool) and 'content' (string or array of content parts for vision)."
    },
    {
      "name": "temperature",
      "type": "number",
      "required": false,
      "description": "Sampling temperature between 0 and 2. Lower values are more deterministic (default 1)."
    },
    {
      "name": "top_p",
      "type": "number",
      "required": false,
      "description": "Nucleus sampling parameter (default 1)."
    },
    {
      "name": "max_tokens",
      "type": "integer",
      "required": false,
      "description": "Maximum number of tokens to generate in the response."
    },
    {
      "name": "stream",
      "type": "boolean",
      "required": false,
      "description": "If true, returns a stream of server-sent events (default false)."
    },
    {
      "name": "tools",
      "type": "array",
      "required": false,
      "description": "List of tools the model may call, each with type 'function' and a function definition."
    },
    {
      "name": "response_format",
      "type": "object",
      "required": false,
      "description": "Set to {\"type\": \"json_object\"} to enable JSON mode."
    },
    {
      "name": "n",
      "type": "integer",
      "required": false,
      "description": "Number of completions to generate (default 1)."
    },
    {
      "name": "stop",
      "type": "string or array",
      "required": false,
      "description": "Up to 4 sequences where the model will stop generating."
    }
  ],
  "request_example": {
    "method": "POST",
    "url": "https://api.openai.com/v1/chat/completions",
    "headers": {
      "Authorization": "Bearer sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
      "Content-Type": "application/json"
    },
    "body": {
      "model": "gpt-4o",
      "messages": [
        {"role": "system", "content": "You are a helpful assistant."},
        {"role": "user", "content": "What are the three laws of thermodynamics?"}
      ],
      "temperature": 0.7,
      "max_tokens": 500
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "id": "chatcmpl-9a8b7c6d5e4f",
      "object": "chat.completion",
      "created": 1709164800,
      "model": "gpt-4o-2024-08-06",
      "choices": [
        {
          "index": 0,
          "message": {
            "role": "assistant",
            "content": "The three laws of thermodynamics are:\n\n1. **First Law:** Energy cannot be created or destroyed.\n2. **Second Law:** Entropy always increases.\n3. **Third Law:** Absolute zero is unattainable."
          },
          "finish_reason": "stop"
        }
      ],
      "usage": {
        "prompt_tokens": 28,
        "completion_tokens": 142,
        "total_tokens": 170
      }
    }
  }
}
