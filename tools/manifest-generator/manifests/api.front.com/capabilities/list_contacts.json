{
  "name": "list_contacts",
  "description": "Retrieve all contacts in your Front address book. Returns name, email handles, phone numbers, and custom fields.",
  "endpoint": "/contacts",
  "method": "GET",
  "parameters": [
    { "name": "q", "type": "string", "description": "Search query to filter contacts by name or handle.", "required": false, "example": "john" },
    { "name": "page_token", "type": "string", "description": "Pagination token for fetching the next page.", "required": false, "example": "next_page_token" },
    { "name": "limit", "type": "integer", "description": "Maximum number of contacts to return. Default 50.", "required": false, "example": 25 }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://api2.frontapp.com/contacts?q=john&limit=25",
    "headers": {
      "Authorization": "Bearer {api_key}",
      "Accept": "application/json"
    },
    "body": null
  },
  "response_example": {
    "status": 200,
    "body": {
      "_results": [
        {
          "id": "crd_abc123",
          "name": "John Doe",
          "description": "Premium customer",
          "handles": [
            { "handle": "john@example.com", "source": "email" }
          ],
          "groups": [],
          "custom_fields": {}
        }
      ],
      "_pagination": {
        "next": "https://api2.frontapp.com/contacts?page_token=next_page_token"
      }
    }
  }
}
