{
  "name": "read_secret",
  "description": "Read a secret from a KV (key-value) secrets engine. Supports both KV v1 and v2 engines. Returns the secret data along with metadata like version and creation time.",
  "endpoint": "/v1/{mount}/data/{path}",
  "method": "GET",
  "parameters": [
    { "name": "mount", "type": "string", "description": "The secrets engine mount path.", "required": true, "example": "secret" },
    { "name": "path", "type": "string", "description": "The secret path within the mount.", "required": true, "example": "data/myapp/config" },
    { "name": "version", "type": "number", "description": "Specific version to read (KV v2 only).", "required": false, "example": 1 }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://vault.example.com:8200/v1/secret/data/myapp/config",
    "headers": { "X-Vault-Token": "{vault_token}" }
  },
  "response_example": {
    "status": 200,
    "body": {
      "data": {
        "data": { "database_url": "postgres://user:pass@host:5432/db", "api_key": "sk-xxxx" },
        "metadata": { "created_time": "2024-01-15T10:00:00Z", "version": 3 }
      }
    }
  }
}
