{
  "name": "list_countries",
  "description": "Retrieve a list of all countries supported by the AirVisual API that have air quality monitoring data. Use this as the first step in a location drill-down: list countries, then states within a country, then cities within a state. The country names returned here must be used exactly when calling other endpoints.",
  "endpoint": "/v2/countries",
  "method": "GET",
  "parameters": [
    {
      "name": "key",
      "type": "string",
      "description": "Your API key for authentication.",
      "required": true,
      "example": "{api_key}"
    }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://api.airvisual.com/v2/countries?key={api_key}",
    "headers": {}
  },
  "response_example": {
    "status": 200,
    "body": {
      "status": "success",
      "data": [
        { "country": "Australia" },
        { "country": "Canada" },
        { "country": "China" },
        { "country": "France" },
        { "country": "Germany" },
        { "country": "India" },
        { "country": "Japan" },
        { "country": "USA" }
      ]
    }
  }
}
