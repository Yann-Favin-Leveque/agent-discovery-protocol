{
  "name": "list_states",
  "description": "Retrieve a list of states or provinces within a specified country that have air quality monitoring data. This is the second step in a location drill-down after listing countries. The state names returned here must be used exactly when calling the list_cities or get_city_data endpoints.",
  "endpoint": "/v2/states",
  "method": "GET",
  "parameters": [
    {
      "name": "key",
      "type": "string",
      "description": "Your API key for authentication.",
      "required": true,
      "example": "{api_key}"
    },
    {
      "name": "country",
      "type": "string",
      "description": "Country name (must match exactly as returned by the list_countries endpoint).",
      "required": true,
      "example": "USA"
    }
  ],
  "request_example": {
    "method": "GET",
    "url": "https://api.airvisual.com/v2/states?country=USA&key={api_key}",
    "headers": {}
  },
  "response_example": {
    "status": 200,
    "body": {
      "status": "success",
      "data": [
        { "state": "Alabama" },
        { "state": "Alaska" },
        { "state": "Arizona" },
        { "state": "California" },
        { "state": "Colorado" },
        { "state": "New York" },
        { "state": "Texas" },
        { "state": "Washington" }
      ]
    }
  }
}
