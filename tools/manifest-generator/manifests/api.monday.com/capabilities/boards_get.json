{
  "name": "boards_get",
  "description": "Get detailed information about a specific board.",
  "resource_group": "boards",
  "endpoint": "/",
  "method": "POST",
  "parameters": [
    {
      "name": "board_id",
      "type": "string",
      "required": true,
      "location": "body",
      "description": "The ID of the board"
    }
  ],
  "auth_scopes": [
    "boards:read",
    "boards:write",
    "workspaces:read",
    "workspaces:write"
  ],
  "request_example": {
    "query": "{ boards (ids: [123]) { id name columns { id title type settings_str } groups { id title color } owner { id name } } }"
  }
}