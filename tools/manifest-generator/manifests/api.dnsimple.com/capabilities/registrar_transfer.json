{
  "name": "registrar_transfer",
  "description": "Manage registrar transfer — enable domain transfer lock, domain transfer lock status, retrieve a domain transfer. Supports standard CRUD operations through the REST API.",
  "endpoint": "/{account}/registrar/domains/{domain}/transfer_lock",
  "method": "POST",
  "parameters": [],
  "request_example": {
    "method": "POST",
    "url": "https://api.dnsimple.com/v2/{account}/registrar/domains/{domain}/transfer_lock",
    "headers": {
      "Content-Type": "application/json",
      "Authorization": "Bearer {access_token}"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {}
    }
  }
}
