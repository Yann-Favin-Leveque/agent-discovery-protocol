{
  "name": "analytics",
  "description": "Manage analytics — create an analytics event, flush in-memory analytics to disk, create analytics rule(s). Supports standard CRUD operations through the REST API.",
  "endpoint": "/analytics/events",
  "method": "POST",
  "parameters": [],
  "request_example": {
    "method": "POST",
    "url": "{protocol}://{hostname}:{port}/analytics/events",
    "headers": {
      "Content-Type": "application/json",
      "X-TYPESENSE-API-KEY": "{api_key}"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {}
    }
  }
}
