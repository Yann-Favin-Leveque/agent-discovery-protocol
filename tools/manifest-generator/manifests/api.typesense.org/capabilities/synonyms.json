{
  "name": "synonyms",
  "description": "Manage synonyms — create or update a synonym set, create or update a synonym set item, list all synonym sets. Supports standard CRUD operations through the REST API.",
  "endpoint": "/synonym_sets/{synonymSetName}",
  "method": "PUT",
  "parameters": [
    {
      "name": "synonymSetName",
      "type": "string",
      "description": "The name of the synonym set to create/update",
      "required": true,
      "example": "Example"
    }
  ],
  "request_example": {
    "method": "PUT",
    "url": "{protocol}://{hostname}:{port}/synonym_sets/{synonymSetName}",
    "headers": {
      "Content-Type": "application/json",
      "X-TYPESENSE-API-KEY": "{api_key}"
    },
    "body": {
      "synonymSetName": "Example"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "success": true,
      "data": {
        "synonymSetName": "Example"
      }
    }
  }
}
