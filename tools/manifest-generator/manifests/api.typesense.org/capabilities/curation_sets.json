{
  "name": "curation_sets",
  "description": "Manage curation sets — create or update a curation set, create or update a curation set item, list all curation sets. Supports standard CRUD operations through the REST API.",
  "endpoint": "/curation_sets/{curationSetName}",
  "method": "PUT",
  "parameters": [
    {
      "name": "curationSetName",
      "type": "string",
      "description": "The name of the curation set to create/update",
      "required": true,
      "example": "Example"
    }
  ],
  "request_example": {
    "method": "PUT",
    "url": "{protocol}://{hostname}:{port}/curation_sets/{curationSetName}",
    "headers": {
      "Content-Type": "application/json",
      "X-TYPESENSE-API-KEY": "{api_key}"
    },
    "body": {
      "curationSetName": "Example"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "success": true,
      "data": {
        "curationSetName": "Example"
      }
    }
  }
}
