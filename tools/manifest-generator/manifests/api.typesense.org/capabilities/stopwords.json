{
  "name": "stopwords",
  "description": "Manage stopwords — upserts a stopwords set, retrieves all stopwords sets, retrieves a stopwords set. Supports standard CRUD operations through the REST API.",
  "endpoint": "/stopwords/{setId}",
  "method": "PUT",
  "parameters": [
    {
      "name": "setId",
      "type": "string",
      "description": "The ID of the stopwords set to upsert.",
      "required": true,
      "example": "countries"
    }
  ],
  "request_example": {
    "method": "PUT",
    "url": "{protocol}://{hostname}:{port}/stopwords/{setId}",
    "headers": {
      "Content-Type": "application/json",
      "X-TYPESENSE-API-KEY": "{api_key}"
    },
    "body": {
      "setId": "countries"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "success": true,
      "data": {
        "setId": "countries"
      }
    }
  }
}
