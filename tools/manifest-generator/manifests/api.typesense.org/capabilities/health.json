{
  "name": "health",
  "description": "Manage health — checks if typesense server is ready to accept requests. Supports standard CRUD operations through the REST API.",
  "endpoint": "/health",
  "method": "GET",
  "parameters": [],
  "request_example": {
    "method": "GET",
    "url": "{protocol}://{hostname}:{port}/health",
    "headers": {
      "Content-Type": "application/json",
      "X-TYPESENSE-API-KEY": "{api_key}"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "success": true,
      "data": {}
    }
  }
}
