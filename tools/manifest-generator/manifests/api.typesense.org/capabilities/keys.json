{
  "name": "keys",
  "description": "Manage keys — create an api key, retrieve (metadata about) all keys, retrieve (metadata about) a key. Supports standard CRUD operations through the REST API.",
  "endpoint": "/keys",
  "method": "POST",
  "parameters": [],
  "request_example": {
    "method": "POST",
    "url": "{protocol}://{hostname}:{port}/keys",
    "headers": {
      "Content-Type": "application/json",
      "X-TYPESENSE-API-KEY": "{api_key}"
    }
  },
  "response_example": {
    "status": 201,
    "body": {
      "success": true,
      "data": {}
    }
  }
}
