{
  "name": "custom_domains_list",
  "description": "List custom domains for a bucket.",
  "resource_group": "custom_domains",
  "endpoint": "/accounts/{account_id}/r2/buckets/{bucket_name}/domains/custom",
  "method": "GET",
  "parameters": [
    {
      "name": "account_id",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Cloudflare account identifier"
    },
    {
      "name": "bucket_name",
      "type": "string",
      "required": true,
      "location": "path",
      "description": "Name of the R2 bucket"
    }
  ],
  "auth_scopes": [
    "read",
    "write"
  ]
}