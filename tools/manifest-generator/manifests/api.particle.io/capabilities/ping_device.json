{
  "name": "ping_device",
  "description": "Check whether a specific device is currently online and reachable. Sends a ping to the device and returns whether it responded. Useful for verifying connectivity before attempting to read variables or call functions.",
  "endpoint": "/v1/devices/{deviceId}/ping",
  "method": "PUT",
  "parameters": [
    {
      "name": "deviceId",
      "type": "string",
      "description": "The unique ID of the device to ping.",
      "required": true,
      "example": "0123456789abcdef01234567"
    }
  ],
  "request_example": {
    "method": "PUT",
    "url": "https://api.particle.io/v1/devices/0123456789abcdef01234567/ping",
    "headers": {
      "Authorization": "Bearer {access_token}"
    }
  },
  "response_example": {
    "status": 200,
    "body": {
      "online": true,
      "ok": true
    }
  }
}
