{
  "domain": "sheets.googleapis.com",
  "display_name": "Google Sheets API",
  "auth_type": "oauth2",
  "portal_url": "https://console.cloud.google.com/apis/credentials",
  "steps": [
    "Go to https://console.cloud.google.com and log in with your Google account",
    "Create a new project or select an existing one",
    "Go to APIs & Services > Library and search for 'Google Sheets API', then click 'Enable'",
    "Also enable 'Google Drive API' if you need drive-level access",
    "Go to APIs & Services > OAuth consent screen and configure it (set User Type to 'External' for public apps, add your app name and contact email)",
    "Go to APIs & Services > Credentials at https://console.cloud.google.com/apis/credentials",
    "Click 'Create Credentials' > 'OAuth client ID'",
    "Select 'Web application' as the application type, enter a name, and add your redirect URI",
    "Click 'Create' and copy both the Client ID and Client Secret from the dialog"
  ],
  "credential_fields": [
    {
      "name": "client_id",
      "label": "Client ID",
      "description": "OAuth 2.0 Client ID from Google Cloud Console",
      "secret": false,
      "placeholder": "YOUR_CLIENT_ID.apps.googleusercontent.com"
    },
    {
      "name": "client_secret",
      "label": "Client Secret",
      "description": "OAuth 2.0 Client Secret from Google Cloud Console",
      "secret": true,
      "placeholder": "YOUR_CLIENT_SECRETxxxxxxxx"
    }
  ],
  "notes": [
    "Both Google Sheets API and Google Drive API must be enabled in the API Library",
    "The OAuth consent screen must be configured before credentials can be used",
    "For testing, add your Google account as a test user under the OAuth consent screen",
    "Client secret is shown only at creation time; you can download the JSON file from the credentials page",
    "Required scopes: spreadsheets (read/write) and optionally drive (for file management)",
    "Google Sheets API has no per-request cost; standard Google Workspace quotas apply"
  ],
  "last_verified": "2026-03-02"
}
