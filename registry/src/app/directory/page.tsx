import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Directory — AgentDNS" };

export default function DirectoryPage() {
  return (
    <PlaceholderPage
      title="Service Directory"
      subtitle="Browse and search all indexed services and their capabilities."
    />
  );
}
