import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Submit — AgentDNS" };

export default function SubmitPage() {
  return (
    <PlaceholderPage
      title="Submit Your Service"
      subtitle="Register your API's /.well-known/agent endpoint in the directory."
    />
  );
}
