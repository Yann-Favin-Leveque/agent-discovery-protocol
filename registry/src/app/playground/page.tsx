import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Playground — AgentDNS" };

export default function PlaygroundPage() {
  return (
    <PlaceholderPage
      title="Manifest Validator"
      subtitle="Paste your /.well-known/agent URL and validate it against the spec."
    />
  );
}
